<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <% if @item[:part] != nil %>
    <title><%= @item[:title] %> (<%= @item[:part] %>) | <%= @config[:site_name] %></title>
    <% else %>
    <title><%= @item[:title] %> | <%= @config[:site_name] %></title>
    <% end %>

    <link rel="stylesheet" href="/_assets/style.css">
    <link rel="stylesheet" href="/_assets/tachyons.css">
    <link rel="stylesheet" href="/_assets/katex/katex.min.css">

    <link rel="me" href="https://mathstodon.xyz/@thosgood"></a>
</head>
